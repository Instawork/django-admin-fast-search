============
Installation
============

At the command line::

    $ easy_install django-admin-fast-search

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-admin-fast-search
    $ pip install django-admin-fast-search
